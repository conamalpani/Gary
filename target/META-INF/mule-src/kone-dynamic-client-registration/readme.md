Environment configuration file (e.g. dev.yaml) requires the following changes:

* host - Salesforce host where Connected Apps are registered
* authcode - Initial access token from Parent Connected App in Salesforce
